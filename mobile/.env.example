# Run `ipconfig` in PowerShell, use the IPv4 address of your active Wi-Fi adapter.
# Phone (Expo Go) and this machine must be on the same Wi-Fi network.
# Do NOT use localhost/127.0.0.1 here -- that resolves to the phone itself, not this machine.
EXPO_PUBLIC_API_URL=http://192.168.1.23:5000
